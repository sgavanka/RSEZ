<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="app.rsez">

    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:title=".MainActivity" android:name=".features.login.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
            android:name=".features.home.HomeActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".features.events.EventCreateActivity"
            android:label="Create Event" />
        <activity
            android:name=".features.login.RegisterActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".features.events.EventEditFragment"
            android:label="RSEZ" />
        <activity
            android:name=".features.events.InviteActivity"
            android:label="RSEZ" />
        <activity
            android:name=".features.events.EventDetailsActivity"
            android:label="RSEZ" />
        <activity
            android:name=".QRScanFragment"
            android:label="RSEZ" />
    </application>


</manifest>